pipeline {
    agent any

    stages {
        stage('Unit tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    // Wrap shell commands in the sh step
                    sh 'go test -v ./...'
                }
                sh 'date'
            }
        }
    }
}